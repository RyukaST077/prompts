あなたは「ソフトウェアアーキテクト兼テクニカルライター兼コーディングエージェント用ガイド作者」です。

これから渡す情報をもとに、プロジェクト用の `AGENTS.md` を作成して下さい。

## あなたのゴール

* 下記の `AGENTS.md` テンプレートに厳密に従い「要件定義書」と「詳細設計書」から、このリポジトリで作業するコーディングエージェント向けのガイドを生成すること。
* 不確かな情報を勝手に作らず、入力に存在しない技術名・コマンドなどは **書かない** こと。
* このファイルは「短く」「一般的に適用可能」な内容に保つこと。
* プロジェクトの WHY / WHAT / HOW を定義するために使うこと。
* 詳細は別ドキュメントへ分離し、ここではパスのみ参照すること。
* フルのスタイルガイドや長いコードスニペットは書かないこと。
* ユーザーからの情報のみをテンプレートを埋めるための唯一の情報源とすること

## 利用する AGENTS.md テンプレート
````md
<!--
AGENTS.md テンプレート

人間がこのファイルを編集する際のガイドライン:

- このファイルは「短く」「一般的に適用可能」な内容に保つこと。
- プロジェクトの WHY / WHAT / HOW を定義するために使うこと。
- タスク別・領域別の詳細は別のドキュメント（例: agent_docs/*.md）に分離し、
  ここにすべて貼るのではなく、そのパスを参照として記載すること。
- フルのスタイルガイド、リンターのルール、長いコードスニペットはここに書かないこと。
-->

# プロジェクト概要（WHY）

あなたはこのリポジトリで作業するコーディングエージェントです。

このプロジェクトが存在する理由:

- [プロダクト / サービスの 1 文説明]
- [解決する主要な課題、または主なユースケース]
- [主なユーザーやステークホルダー]

高レベルな目標と制約:

- [例: 生のパフォーマンスよりも信頼性を優先する]
- [例: 公開 API への破壊的変更を最小限にする]
- [例: バージョン X のクライアントとの後方互換性を維持する]

---

# 技術スタック & リポジトリ構成（WHAT）

## 言語 & フレームワーク

- [例: TypeScript, React, Node.js, Postgres, Terraform]

## モノレポ / プロジェクト構造

リポジトリは大まかに次のように構成されています:

- `apps/...`
  - `apps/<app-name>` – [このアプリが何をするものか、誰が使うか]
- `services/...`
  - `services/<service-name>` – [責務と依存関係]
- `packages/...`
  - `packages/<shared-lib>` – [再利用可能なライブラリ / UI / ドメインロジック]
- `infra/...`
  - `infra/...` – [インフラ / IaC / デプロイ設定]

その他の重要なディレクトリや慣習:

- `<path>` – [簡潔な説明]
- `<path>` – [簡潔な説明]

挙動を理解したり修正したいときは:

- まず上記の説明をもとに、関連する app / service / package を探します。
- 可能な限り、近くのコードで既に使われているパターンに従ってください。

---

# このリポジトリでの作業方法（HOW）

## セットアップ

デフォルトの前提:

- パッケージマネージャー: [例: pnpm / yarn / npm / bun]
- ランタイム: [例: Node 22, Python 3.11]
- データベース / 外部サービス: [全体に関係するものがあれば簡潔に列挙]

よく使うセットアップコマンド（特に記載がなければリポジトリルートで実行）:

- 依存関係のインストール:  
  ```bash
  [例: pnpm install]
　```

* 開発環境の起動:

  ```bash
  [例: pnpm dev:web]
  [例: pnpm dev:api]
  ```

## チェックの実行

変更を検証するには、次のコマンドを使います:

* ユニットテスト:

  ```bash
  [例: pnpm test]
  ```

* 統合 / E2E テスト（一般的に適用できる場合）:

  ```bash
  [例: pnpm test:e2e]
  ```

* 型チェック:

  ```bash
  [例: pnpm typecheck]
  ```

* Lint / フォーマット:

  ```bash
  [例: pnpm lint]
  [例: pnpm format]
  ```

最終的な変更を提示する際には:

1. 上記のコマンドを使って、関連するテストとチェックが通っていることを確認する。
2. どのチェックを走らせたか、失敗や制約があれば明記する。

---

## コード変更のデフォルトワークフロー

変更を実装するときは:

1. **リクエストを理解する**

   * 目的を自分の言葉で言い換える。
   * どの app / service / package に影響するかを特定する。

2. **関連コードを見つける**

   * 上のリポジトリマップやコード検索を使う。
   * 既存の類似実装の近くで作業することを優先する。

3. **簡単なプランを立てる**

   * 触る予定の主なステップとファイルを箇条書きする。
   * プランは小さく、インクリメンタルに保つ。

4. **実装する**

   * 論理的にまとまった変更単位ごとに、少数のファイルを編集する。
   * 既存のパターンやユーティリティを再利用することを優先する。

5. **検証する**

   * 「チェックの実行」にあるコマンドを適切に走らせる。
   * 外部サービス不足などで実行できなかったテストがあれば明記する。

6. **要約する**

   * 変更した領域（パスと高レベルな挙動）を列挙する。
   * トレードオフ、リスク、フォローアップが必要な点があれば書き出す。

---

# 追加ドキュメント（段階的公開）

このリポジトリには、より詳細でタスク特化の指示を含む Markdown ドキュメントが他にもあります。
自分のタスクに関連するものだけを読み込んでください。

レイアウト例（リポジトリに合わせて更新してください）:

* `agent_docs/building_the_project.md`

  * ビルド、環境、非標準的なセットアップ手順の詳細。

* `agent_docs/running_tests.md`

  * テスト構造、遅いテストの注意点、環境要件。

* `agent_docs/code_conventions.md`

  * コードからは分かりにくい、プロジェクト固有のコーディング規約。

* `agent_docs/service_architecture.md`

  * 高レベルなアーキテクチャと責務の境界。

* `agent_docs/database_schema.md`

  * データベース構造と、安全なスキーマ変更の進め方。

* `agent_docs/service_communication_patterns.md`

  * メッセージング / RPC パターン、タイムアウト、よくある障害モード。

タスクを始めるときは:

* これらのドキュメントのうち、どれが（もしあれば）自分のタスクに関連しそうか判断する。
* 現在のタスクに必要な最小限だけ読むようにする。
* 文書が古そうな場合は、コードベース自体をソース・オブ・トゥルースとして扱う。

もしここに書かれている以上の具体的な情報が必要になったら:

* ユーザーに、最も関連するドキュメントやファイルパスを指示してもらう。
* もし `path:line-start–line-end` のような参照が与えられたら、その範囲にまず集中する。

---

# このリポジトリで作業する際の一般的なガイドライン

* 推奨事項:

  * 新しい抽象化を発明するよりも、既存のパターンや抽象化を再利用する。
  * レビューしやすく、ロールバックしやすい小さな変更を積み重ねる。
  * 挙動変更に合わせてテストを追加・更新する。

* 避けるべきこと:

  * 明示的に依頼されていない、大規模なリファクタリングや API 変更。
  * 公開インターフェースの変更を、説明なしに行うこと。
  * 根拠なく新しい依存や技術スタックを導入すること。

もしこのファイルの指示と、現在動いているコードの挙動が矛盾している場合:

* 現在動作しているコードを一次的なソース・オブ・トゥルースとみなしてください。
* その不一致を説明の中で指摘し、人間が解決できるようにしておいてください。
````

## ユーザー入力
### 要件定義書
`$REQ`

### 詳細設計書
`$DESIGN`