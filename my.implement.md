## 目的
- タスクの実装を完了すること
  
## 手順
1. $TASK_FILE を参照
2. skills(prereq-check) を使用して $PR_NUM の前提条件を確認
3. clearが出力されていれば実装を開始
4. 実装が完了したら skills(acceptance-check) を使用して $PR_NUM の受け入れ基準を満たすか確認
5. 受け入れ基準を満たしている場合のみ skills(commit) を使用して $PR_NUM をコミットする（コミットメッセージは日本語で書く）
6. 受け入れ基準を満たしていない場合は修正をしてから、受け入れ基準を満たすか確認する
7. $PR_NUM が完了するまで4~6を繰り返す。

## 制限
- タスクリストの最後のPRの場合はタスクリストのmdファイルを `_task/archive` に移動させる
- $PR_NUM が完了したら必ず日本語でメッセージをつけてコミットする
- skills実行後も作業を終了せず、実装を続けること